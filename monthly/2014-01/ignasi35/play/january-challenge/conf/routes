# Routes
# This file defines all FlightsController routes (Higher priority routes first)
# ~~~~

# Home page
POST    /flights                     controllers.FlightsController.addFlight
GET     /flights                     controllers.FlightsController.getFlights
GET     /flights/from/:cityCode      controllers.FlightsController.getFlightsFrom(cityCode)
GET     /flights/to/:cityCode        controllers.FlightsController.getFlightsTo(cityCode)
PUT     /flights                     controllers.FlightsController.updateFlight

